require 'spec_helper'

describe PigLatinator do
  it 'has a version number' do
    expect(PigLatinator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
